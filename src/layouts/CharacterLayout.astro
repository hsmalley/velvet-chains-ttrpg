---
const { name, class: charClass, level, system, alignment, tags } = Astro.props.entry.data;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{name} — Velvet Chains</title>
  </head>
  <body>
    <main data-pagefind-body>
      <h1>🧙 {name}</h1>
      <h2>Level {level} {charClass}</h2>

      <table>
        <tr><th>System</th><td>{system}</td></tr>
        <tr><th>Alignment</th><td>{alignment}</td></tr>
        <tr><th>Tags</th><td>{tags.join(', ')}</td></tr>
      </table>

      <slot />
    </main>
  </body>
</html>
